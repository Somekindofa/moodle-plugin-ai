function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
define(["exports", "core/ajax"], function (_exports, Ajax) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.init = void 0;
  Ajax = _interopRequireWildcard(Ajax);
  function _interopRequireWildcard(e, t) { if ("function" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function _interopRequireWildcard(e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, default: e }; if (null === e || "object" != _typeof(e) && "function" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (var _t3 in e) "default" !== _t3 && {}.hasOwnProperty.call(e, _t3) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, _t3)) && (i.get || i.set) ? o(f, _t3, i) : f[_t3] = e[_t3]); return f; })(e, t); }
  function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i.return) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
  function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); } r ? i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n : (o("next", 0), o("throw", 1), o("return", 2)); }, _regeneratorDefine2(e, r, n, t); }
  function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
  function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; } // Standard license block omitted for brevity
  /**
 * @module     block_aiassistant/chat_interface
 * @copyright  2025 Your Name
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
  /**
   * Initialize the AI chat interface
   */
  var init = _exports.init = function init() {
    // Function to initialize the chat interface
    var initializeChat = function initializeChat() {
      console.log('AI Chat: Initializing...');
      var sendButton = document.getElementById("ai-chat-send");
      var chatInput = document.getElementById("ai-chat-input");
      var messagesContainer = document.getElementById("ai-chat-messages");
      var chatContainer = document.getElementById("ai-chat-container");
      var resizeHandle = document.getElementById("ai-resize-handle");
      var providerSelect = document.getElementById("ai-provider-select");
      var claudeModelSelect = document.getElementById("claude-model-select");
      var claudeModelSelection = document.getElementById("claude-model-selection");
      console.log('AI Chat: Elements found:', {
        sendButton: !!sendButton,
        chatInput: !!chatInput,
        messagesContainer: !!messagesContainer,
        chatContainer: !!chatContainer,
        resizeHandle: !!resizeHandle,
        providerSelect: !!providerSelect,
        claudeModelSelect: !!claudeModelSelect
      });
      if (!sendButton || !chatInput || !messagesContainer || !providerSelect) {
        console.error('AI Chat: Required elements not found');
        return;
      }

      // Global configuration storage
      var aiConfig = null;
      var currentProvider = 'fireworks';
      var selectedClaudeModel = '';

      // Load AI configuration on startup
      loadAIConfiguration();

      // Load saved dimensions from localStorage
      var savedWidth = localStorage.getItem('ai-chat-width');
      var savedHeight = localStorage.getItem('ai-chat-height');
      if (savedWidth) {
        chatContainer.style.width = savedWidth + 'px';
      }
      if (savedHeight) {
        messagesContainer.style.height = savedHeight + 'px';
      }

      // Resize functionality
      var isResizing = false;
      var startX, startY, startWidth, startHeight;
      if (resizeHandle) {
        resizeHandle.addEventListener('mousedown', function (e) {
          isResizing = true;
          chatContainer.classList.add('resizing');
          startX = e.clientX;
          startY = e.clientY;
          startWidth = parseInt(window.getComputedStyle(chatContainer).width, 10);
          startHeight = parseInt(window.getComputedStyle(messagesContainer).height, 10);
          e.preventDefault();
        });
      }
      document.addEventListener('mousemove', function (e) {
        if (!isResizing) {
          return;
        }
        var newWidth = Math.max(300, Math.min(800, startWidth + (e.clientX - startX)));
        var newHeight = Math.max(150, Math.min(600, startHeight + (e.clientY - startY)));
        chatContainer.style.width = newWidth + 'px';
        messagesContainer.style.height = newHeight + 'px';
        // Save to localStorage
        localStorage.setItem('ai-chat-width', newWidth);
        localStorage.setItem('ai-chat-height', newHeight);
      });
      document.addEventListener('mouseup', function () {
        if (isResizing) {
          isResizing = false;
          chatContainer.classList.remove('resizing');
        }
      });

      /**
       * Load AI configuration from backend
       */
      function loadAIConfiguration() {
        console.log('DEBUG: Starting AJAX call to block_aiassistant_get_ai_config');
        Ajax.call([{
          methodname: 'block_aiassistant_get_ai_config',
          args: {},
          done: function done(config) {
            console.log('DEBUG: AJAX success, config received:', config);
            if (config && config.success) {
              aiConfig = config;
              console.log('AI Config loaded successfully:', aiConfig);
              setupProviderUI();
            } else {
              console.error('Failed to load AI configuration:', config ? config.message : 'No response received');
              console.error('Full config object:', config);
              aiConfig = {
                success: false,
                claude_available: false,
                fireworks_available: false,
                claude_models: [],
                default_claude_model: ''
              };
              showConfigurationError('Failed to load AI configuration. Please check plugin settings.');
            }
          },
          fail: function fail(error) {
            console.error('AJAX call failed:', error);
            console.error('Error details:', {
              name: error.name,
              message: error.message,
              stack: error.stack
            });
            aiConfig = {
              success: false,
              claude_available: false,
              fireworks_available: false,
              claude_models: [],
              default_claude_model: ''
            };
            showConfigurationError('Could not connect to AI configuration service.');
          }
        }]);
      }

      /**
       * Show configuration error in chat
       */
      function showConfigurationError(message) {
        var errorDiv = document.createElement("div");
        errorDiv.className = "ai-message";
        errorDiv.innerHTML = "<strong>AI Assistant:</strong> <em>Error: ".concat(message, "</em>");
        messagesContainer.appendChild(errorDiv);
      }

      /**
       * Setup provider UI based on configuration
       */
      function setupProviderUI() {
        console.log('DEBUG: setupProviderUI called with aiConfig:', aiConfig);

        // Ensure aiConfig exists before proceeding
        if (!aiConfig) {
          console.error('AI configuration not loaded, cannot setup provider UI');
          return;
        }
        console.log('DEBUG: aiConfig properties:', {
          success: aiConfig.success,
          claude_available: aiConfig.claude_available,
          fireworks_available: aiConfig.fireworks_available,
          default_claude_model: aiConfig.default_claude_model,
          claude_models: aiConfig.claude_models
        });

        // Clear existing options
        providerSelect.innerHTML = '';
        if (claudeModelSelect) {
          claudeModelSelect.innerHTML = '';
        }
        var hasAvailableProvider = false;

        // Add available providers
        if (aiConfig && aiConfig.fireworks_available) {
          var fireworksOption = document.createElement('option');
          fireworksOption.value = 'fireworks';
          fireworksOption.textContent = 'Fireworks.ai';
          providerSelect.appendChild(fireworksOption);
          hasAvailableProvider = true;
        }
        if (aiConfig && aiConfig.claude_available) {
          var claudeOption = document.createElement('option');
          claudeOption.value = 'claude';
          claudeOption.textContent = 'Claude API';
          providerSelect.appendChild(claudeOption);
          hasAvailableProvider = true;

          // Populate Claude models if element exists and models are available
          if (claudeModelSelect && aiConfig.claude_models && Array.isArray(aiConfig.claude_models)) {
            aiConfig.claude_models.forEach(function (model) {
              var modelOption = document.createElement('option');
              modelOption.value = model.key;
              modelOption.textContent = model.name;
              claudeModelSelect.appendChild(modelOption);
            });

            // Set default Claude model
            if (aiConfig && aiConfig.default_claude_model) {
              claudeModelSelect.value = aiConfig.default_claude_model;
              selectedClaudeModel = aiConfig.default_claude_model;
            }
          }
        }

        // If no providers are available, add disabled options
        if (!hasAvailableProvider) {
          if (!aiConfig || !aiConfig.fireworks_available) {
            var _fireworksOption = document.createElement('option');
            _fireworksOption.value = 'fireworks';
            _fireworksOption.textContent = 'Fireworks.ai (Not configured)';
            _fireworksOption.disabled = true;
            providerSelect.appendChild(_fireworksOption);
          }
          if (!aiConfig || !aiConfig.claude_available) {
            var _claudeOption = document.createElement('option');
            _claudeOption.value = 'claude';
            _claudeOption.textContent = 'Claude API (Not configured)';
            _claudeOption.disabled = true;
            providerSelect.appendChild(_claudeOption);
          }
          showConfigurationError('No AI providers are configured. Please check plugin settings.');
          return;
        }

        // Restore saved selections
        var savedProvider = localStorage.getItem('ai-chat-provider');
        var savedClaudeModel = localStorage.getItem('ai-chat-claude-model');
        if (savedProvider && document.querySelector("option[value=\"".concat(savedProvider, "\"]"))) {
          providerSelect.value = savedProvider;
          currentProvider = savedProvider;
        } else if (aiConfig && aiConfig.fireworks_available) {
          currentProvider = 'fireworks';
          providerSelect.value = 'fireworks';
        } else if (aiConfig && aiConfig.claude_available) {
          currentProvider = 'claude';
          providerSelect.value = 'claude';
        }
        if (savedClaudeModel && claudeModelSelect && document.querySelector("#claude-model-select option[value=\"".concat(savedClaudeModel, "\"]"))) {
          claudeModelSelect.value = savedClaudeModel;
          selectedClaudeModel = savedClaudeModel;
        }

        // Show/hide Claude model selection
        updateClaudeModelVisibility();
      }

      /**
       * Update Claude model selection visibility
       */
      function updateClaudeModelVisibility() {
        var _aiConfig;
        if (claudeModelSelection && currentProvider === 'claude' && (_aiConfig = aiConfig) !== null && _aiConfig !== void 0 && _aiConfig.claude_available) {
          claudeModelSelection.style.display = 'flex';
        } else if (claudeModelSelection) {
          claudeModelSelection.style.display = 'none';
        }
      }

      // Provider selection change handler
      if (providerSelect) {
        providerSelect.addEventListener('change', function () {
          currentProvider = this.value;
          localStorage.setItem('ai-chat-provider', currentProvider);
          updateClaudeModelVisibility();

          // Clear chat messages when switching providers
          messagesContainer.innerHTML = "\n                    <div class=\"ai-message\">\n                        <strong>AI Assistant:</strong> Hello! I'm now using ".concat(currentProvider === 'claude' ? 'Claude API' : 'Fireworks.ai', ". How can I help you today?\n                    </div>\n                ");
        });
      }

      // Claude model selection change handler
      if (claudeModelSelect) {
        claudeModelSelect.addEventListener('change', function () {
          selectedClaudeModel = this.value;
          localStorage.setItem('ai-chat-claude-model', selectedClaudeModel);
        });
      }

      /**
       * Send a message to the AI assistant
       */
      function sendMessage() {
        var message = chatInput.value.trim();
        if (!message) {
          return;
        }

        // Check if we have a valid configuration
        if (!aiConfig || !aiConfig.fireworks_available && !aiConfig.claude_available) {
          showConfigurationError('No AI providers are configured. Please check plugin settings.');
          return;
        }

        // Add user message
        var userMessageDiv = document.createElement("div");
        userMessageDiv.className = "user-message";
        userMessageDiv.innerHTML = "<strong>You:</strong> " + message;
        messagesContainer.appendChild(userMessageDiv);

        // Clear input
        chatInput.value = "";

        // Show loading state
        var loadingDiv = document.createElement("div");
        loadingDiv.className = "ai-message";
        loadingDiv.innerHTML = "<strong>AI Assistant:</strong> <em>Getting your credentials for ".concat(currentProvider, "...</em>");
        messagesContainer.appendChild(loadingDiv);

        // Get user credentials via AJAX
        Ajax.call([{
          methodname: 'block_aiassistant_get_user_credentials',
          args: {
            provider: currentProvider
          },
          done: function done(credentials) {
            // Remove loading message
            messagesContainer.removeChild(loadingDiv);
            if (credentials.success) {
              // Now make the actual chat request using those credentials
              if (currentProvider === 'claude') {
                sendClaudeChatMessage(message, credentials.api_key);
              } else {
                sendFireworksChatMessage(message, credentials.api_key);
              }
            } else {
              var errorDiv = document.createElement("div");
              errorDiv.className = "ai-message";
              errorDiv.innerHTML = "<strong>AI Assistant:</strong> <em>Error: " + credentials.message + "</em>";
              messagesContainer.appendChild(errorDiv);
            }
          },
          fail: function fail(error) {
            // Remove loading message
            if (messagesContainer.contains(loadingDiv)) {
              messagesContainer.removeChild(loadingDiv);
            }
            var errorDiv = document.createElement("div");
            errorDiv.className = "ai-message";
            errorDiv.innerHTML = "<strong>AI Assistant:</strong> <em>Failed to get credentials: " + (error.message || 'Unknown error') + "</em>";
            messagesContainer.appendChild(errorDiv);
          }
        }]);

        // Scroll to bottom
        messagesContainer.scrollTop = messagesContainer.scrollHeight;
      }

      /**
       * Send chat message to Fireworks API (Direct API approach)
       * @param {string} message - The message to send
       * @param {string} apiKey - The master API key for authentication
       */
      function sendFireworksChatMessage(_x, _x2) {
        return _sendFireworksChatMessage.apply(this, arguments);
      }
      /**
       * Send chat message to Claude API via server-side proxy
       * @param {string} message - The message to send
       * @param {string} apiKey - The API key for authentication (not used in proxy approach)
       */
      function _sendFireworksChatMessage() {
        _sendFireworksChatMessage = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2(message, apiKey) {
          return _regenerator().w(function (_context2) {
            while (1) switch (_context2.n) {
              case 0:
                setTimeout(/*#__PURE__*/_asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee() {
                  var aiMessageDiv, responseSpan, url, options, response, data, content, htmlContent, _t;
                  return _regenerator().w(function (_context) {
                    while (1) switch (_context.p = _context.n) {
                      case 0:
                        aiMessageDiv = document.createElement("div");
                        aiMessageDiv.className = "ai-message";
                        aiMessageDiv.innerHTML = "<strong>AI Assistant (Fireworks):</strong> <span class='response-text'>Thinking...</span>";
                        messagesContainer.appendChild(aiMessageDiv);
                        responseSpan = aiMessageDiv.querySelector('.response-text');
                        url = 'https://api.fireworks.ai/inference/v1/chat/completions';
                        options = {
                          method: 'POST',
                          headers: {
                            'Authorization': 'Bearer ' + apiKey,
                            'Content-Type': 'application/json'
                          },
                          body: JSON.stringify({
                            "model": "accounts/fireworks/models/llama-v3p1-8b-instruct",
                            "messages": [{
                              "role": "user",
                              "content": message
                            }],
                            "max_tokens": 2000,
                            "prompt_truncate_len": 1500,
                            "temperature": 0.7,
                            "top_p": 1,
                            "top_k": 50,
                            "frequency_penalty": 0,
                            "perf_metrics_in_response": false,
                            "presence_penalty": 0,
                            "repetition_penalty": 1,
                            "mirostat_lr": 0.1,
                            "mirostat_target": 1.5,
                            "n": 1,
                            "ignore_eos": false,
                            "response_format": null,
                            "stream": false,
                            "context_length_exceeded_behavior": "truncate"
                          })
                        };
                        _context.p = 1;
                        _context.n = 2;
                        return fetch(url, options);
                      case 2:
                        response = _context.v;
                        _context.n = 3;
                        return response.json();
                      case 3:
                        data = _context.v;
                        console.log('Fireworks API response:', data);
                        if (data.choices && data.choices[0] && data.choices[0].message) {
                          content = data.choices[0].message.content; // Convert markdown to HTML if marked is available
                          if (typeof marked !== 'undefined' && marked.parse) {
                            htmlContent = marked.parse(content);
                            responseSpan.innerHTML = htmlContent;
                          } else {
                            responseSpan.textContent = content;
                          }
                        } else if (data.error) {
                          responseSpan.textContent = 'Error: ' + (data.error.message || data.error);
                          console.error('Fireworks API error:', data.error);
                        } else {
                          responseSpan.textContent = 'Sorry, I could not process your request. No response from Fireworks API.';
                          console.error('Unexpected Fireworks API response:', data);
                        }
                        _context.n = 5;
                        break;
                      case 4:
                        _context.p = 4;
                        _t = _context.v;
                        console.error('Fireworks API call failed:', _t);
                        responseSpan.textContent = 'Sorry, there was an error processing your request: ' + _t.message;
                      case 5:
                        // Scroll to bottom
                        messagesContainer.scrollTop = messagesContainer.scrollHeight;
                      case 6:
                        return _context.a(2);
                    }
                  }, _callee, null, [[1, 4]]);
                })), 1000);
              case 1:
                return _context2.a(2);
            }
          }, _callee2);
        }));
        return _sendFireworksChatMessage.apply(this, arguments);
      }
      function sendClaudeChatMessage(_x3, _x4) {
        return _sendClaudeChatMessage.apply(this, arguments);
      }
      function _sendClaudeChatMessage() {
        _sendClaudeChatMessage = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee4(message, apiKey) {
          return _regenerator().w(function (_context4) {
            while (1) switch (_context4.n) {
              case 0:
                setTimeout(/*#__PURE__*/_asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee3() {
                  var aiMessageDiv, responseSpan, modelToUse, ajaxPromise, response, content, htmlContent, errorMessage, _errorMessage, _t2;
                  return _regenerator().w(function (_context3) {
                    while (1) switch (_context3.p = _context3.n) {
                      case 0:
                        aiMessageDiv = document.createElement("div");
                        aiMessageDiv.className = "ai-message";
                        aiMessageDiv.innerHTML = "<strong>AI Assistant (Claude):</strong> <span class='response-text'>Thinking...</span>";
                        messagesContainer.appendChild(aiMessageDiv);
                        responseSpan = aiMessageDiv.querySelector('.response-text'); // Determine which model to use with safe fallback
                        modelToUse = selectedClaudeModel;
                        if (!modelToUse && aiConfig && aiConfig.default_claude_model) {
                          modelToUse = aiConfig.default_claude_model;
                        }
                        if (!modelToUse) {
                          modelToUse = 'claude-sonnet-4-20250514'; // hardcoded fallback
                        }
                        _context3.p = 1;
                        // Use server-side proxy to send message to Claude API
                        ajaxPromise = new Promise(function (resolve, reject) {
                          Ajax.call([{
                            methodname: 'block_aiassistant_send_claude_message',
                            args: {
                              message: message,
                              model: modelToUse
                            },
                            done: resolve,
                            fail: reject
                          }]);
                        });
                        _context3.n = 2;
                        return ajaxPromise;
                      case 2:
                        response = _context3.v;
                        console.log('Claude API response:', response);
                        if (response.success && response.content) {
                          content = response.content; // Convert markdown to HTML if marked is available
                          if (typeof marked !== 'undefined' && marked.parse) {
                            htmlContent = marked.parse(content);
                            responseSpan.innerHTML = htmlContent;
                          } else {
                            responseSpan.textContent = content;
                          }
                        } else {
                          errorMessage = response.message || 'Sorry, I could not process your request.';
                          responseSpan.textContent = errorMessage;
                          console.error('Claude API error:', response);
                        }
                        _context3.n = 4;
                        break;
                      case 3:
                        _context3.p = 3;
                        _t2 = _context3.v;
                        console.error('Claude API call failed:', _t2);

                        // Handle specific error types
                        _errorMessage = 'Sorry, there was an error processing your request';
                        if (_t2.message) {
                          if (_t2.message.includes('401')) {
                            _errorMessage = 'Authentication failed - please check API key configuration';
                          } else if (_t2.message.includes('429')) {
                            _errorMessage = 'Rate limit exceeded - please try again later';
                          } else {
                            _errorMessage = "Error: ".concat(_t2.message);
                          }
                        }
                        responseSpan.textContent = _errorMessage;
                      case 4:
                        // Scroll to bottom
                        messagesContainer.scrollTop = messagesContainer.scrollHeight;
                      case 5:
                        return _context3.a(2);
                    }
                  }, _callee3, null, [[1, 3]]);
                })), 1000);
              case 1:
                return _context4.a(2);
            }
          }, _callee4);
        }));
        return _sendClaudeChatMessage.apply(this, arguments);
      }
      sendButton.addEventListener("click", sendMessage);
      chatInput.addEventListener("keypress", function (e) {
        if (e.key === "Enter" && !e.shiftKey) {
          e.preventDefault();
          sendMessage();
        }
      });
    };

    // Try to initialize immediately if DOM is ready, otherwise wait for DOMContentLoaded
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initializeChat);
    } else {
      // DOM is already loaded, initialize immediately
      initializeChat();
    }
  };
});
//# sourceMappingURL=chat_interface.min.js.map
