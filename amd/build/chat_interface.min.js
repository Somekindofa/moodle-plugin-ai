function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
define(["exports", "core/ajax"], function (_exports, Ajax) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.init = void 0;
  Ajax = _interopRequireWildcard(Ajax);
  function _interopRequireWildcard(e, t) { if ("function" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function _interopRequireWildcard(e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, default: e }; if (null === e || "object" != _typeof(e) && "function" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (var _t5 in e) "default" !== _t5 && {}.hasOwnProperty.call(e, _t5) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, _t5)) && (i.get || i.set) ? o(f, _t5, i) : f[_t5] = e[_t5]); return f; })(e, t); }
  function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i.return) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
  function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); } r ? i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n : (o("next", 0), o("throw", 1), o("return", 2)); }, _regeneratorDefine2(e, r, n, t); }
  function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
  function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
  function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
  function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
  function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; } // Standard license block omitted for brevity
  /**
 * @module     block_aiassistant/chat_interface
 * @copyright  2025 Your Name
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
  /**
   * Initialize the AI chat interface
   */
  var init = _exports.init = function init() {
    // Function to initialize the chat interface
    var initializeChat = function initializeChat() {
      console.log('AI Chat: Initializing...');
      var sendButton = document.getElementById("ai-chat-send");
      var chatInput = document.getElementById("ai-chat-input");
      var messagesContainer = document.getElementById("ai-chat-messages");
      var providerSelect = document.getElementById("ai-provider-select");
      var newConversationBtn = document.getElementById("ai-new-conversation-btn");
      var conversationsToggle = document.getElementById("ai-conversations-toggle");
      var conversationsPanel = document.getElementById("ai-conversations-panel");
      var contentArea = document.getElementById("ai-content-area");
      var motto = document.getElementById("ai-motto");
      var resultsArea = document.getElementById("ai-results-area");
      var documentsSection = document.getElementById("ai-documents-section");
      var documentsList = document.getElementById("ai-documents-list");
      if (!sendButton || !chatInput || !messagesContainer || !providerSelect || !newConversationBtn) {
        console.error('AI Chat: Required elements not found');
        return;
      }

      // Global configuration storage
      var aiConfig = null;
      var currentProvider = 'fireworks';
      var currentConversationThreadId = null;

      // Load AI configuration on startup
      loadAIConfiguration();
      loadExistingConversations();

      /**
       * Displays a list of document paths in the documents section.
       * Populates the documents section with the provided document paths.
       * If no documents are provided, displays a "No documents retrieved" message.
       * 
       * @param {string[]} documentPaths - Array of document file paths to display
       */
      function showDocuments(documentPaths) {
        if (documentPaths && documentPaths.length > 0) {
          var listHTML = "\n                    <ul>\n                        ".concat(documentPaths.map(function (path) {
            return "<li>".concat(path, "</li>");
          }).join(''), "\n                    </ul>\n                ");
          documentsList.innerHTML = listHTML;
        } else {
          documentsList.innerHTML = '<p>No documents retrieved yet.</p>';
        }
      }

      /**
       * Show the results area and hide the motto
       */
      function showResultsArea() {
        if (motto) {
          motto.style.display = 'none';
        }
        if (resultsArea) {
          resultsArea.style.display = 'flex';
        }
      }

      /**
       * Hide the results area and show the motto
       */
      function hideResultsArea() {
        if (motto) {
          motto.style.display = 'block';
        }
        if (resultsArea) {
          resultsArea.style.display = 'none';
        }
      }

      /**
       * Load existing conversations from Moodle database
       */
      function loadExistingConversations() {
        Ajax.call([{
          methodname: 'block_aiassistant_manage_conversations',
          args: {
            action: 'list'
          },
          done: function done(result) {
            if (result && result.success && result.conversations) {
              console.log('Existing conversations loaded:', result.conversations);
              populateConversationsList(result.conversations);
            } else {
              console.log('No existing conversations found or failed to load:', result ? result.message : 'No response received');
            }
          },
          fail: function fail(error) {
            console.error('Failed to load existing conversations:', error);
          }
        }]);
      }

      /**
       * Populate the conversations list with existing conversations
       */
      function populateConversationsList(conversations) {
        var conversationList = document.getElementById('ai-conversation-list');

        // Clear existing items
        conversationList.innerHTML = '';

        // Add each conversation to the list
        conversations.forEach(function (conversation) {
          var conversationItem = createConversationElement(conversation.conversation_id, conversation.title);
          conversationList.appendChild(conversationItem);

          // Add click listener to the item
          setupConversationItemListener(conversationItem);
        });
        console.log("Populated ".concat(conversations.length, " conversations in the list"));
      }

      /**
       * Create a conversation element with title and delete button
       */
      function createConversationElement(conversationId, title) {
        var conversationItem = document.createElement('div');
        conversationItem.className = 'ai-conversation-item';
        conversationItem.setAttribute('data-conversation-id', conversationId);
        var titleSpan = document.createElement('span');
        titleSpan.className = 'ai-conversation-title';
        titleSpan.textContent = title;
        var deleteBtn = document.createElement('button');
        deleteBtn.className = 'ai-conversation-delete-btn';
        deleteBtn.textContent = '×';
        deleteBtn.title = 'Delete conversation';
        deleteBtn.type = 'button';

        // Add click handler for delete button
        deleteBtn.addEventListener('click', function (e) {
          e.stopPropagation(); // Prevent conversation selection
          deleteConversation(conversationId, conversationItem);
        });
        conversationItem.appendChild(titleSpan);
        conversationItem.appendChild(deleteBtn);
        return conversationItem;
      }

      /**
       * Delete a conversation with confirmation
       */
      function deleteConversation(conversationId, conversationItem) {
        var conversationTitle = conversationItem.querySelector('.ai-conversation-title').textContent;

        // Show confirmation dialog
        if (!confirm("Are you sure you want to delete \"".concat(conversationTitle, "\"? This will permanently delete the conversation and all its messages."))) {
          return;
        }

        // Call backend to delete conversation
        Ajax.call([{
          methodname: 'block_aiassistant_manage_conversations',
          args: {
            action: 'delete',
            conversation_id: conversationId
          },
          done: function done(result) {
            if (result.success) {
              console.log('Conversation deleted successfully:', conversationId);

              // Check if deleted conversation was the active one
              var wasActive = conversationItem.classList.contains('active');

              // Remove conversation from UI
              conversationItem.remove();

              // If this was the active conversation, create or select another one
              if (wasActive) {
                currentConversationThreadId = null;

                // Check if there are other conversations
                var remainingConversations = document.querySelectorAll('.ai-conversation-item');
                if (remainingConversations.length > 0) {
                  // Select the first remaining conversation
                  remainingConversations[0].click();
                } else {
                  // No conversations left, clear the chat
                  messagesContainer.innerHTML = '<div class="ai-message"><strong>AI Assistant:</strong> Hello! How can I help you today?</div>';
                }
              }
            } else {
              console.error('Failed to delete conversation:', result.message);
              alert('Failed to delete conversation: ' + result.message);
            }
          },
          fail: function fail(error) {
            console.error('Failed to delete conversation:', error);
            alert('Failed to delete conversation. Please try again.');
          }
        }]);
      }

      /**
       * Load AI configuration from backend
       */
      function loadAIConfiguration() {
        Ajax.call([{
          methodname: 'block_aiassistant_get_ai_config',
          args: {},
          done: function done(config) {
            if (config && config.success) {
              aiConfig = config;
              console.log('AI Config loaded successfully.');
              setupProviderUI();
            } else {
              console.error('Failed to load AI configuration:', config ? config.message : 'No response received');
              aiConfig = {
                success: false,
                fireworks_available: false
              };
              showConfigurationError('Failed to load AI configuration. Please check plugin settings.');
            }
          },
          fail: function fail(error) {
            console.error('Error details:', {
              name: error.name,
              message: error.message,
              stack: error.stack
            });
            aiConfig = {
              success: false,
              fireworks_available: false
            };
            showConfigurationError('Could not connect to AI configuration service.');
          }
        }]);
      }

      /**
       * Show configuration error in chat
       */
      function showConfigurationError(message) {
        var errorDiv = document.createElement("div");
        errorDiv.className = "ai-message";
        errorDiv.innerHTML = "<strong>AI Assistant:</strong> <em>Error: ".concat(message, "</em>");
        messagesContainer.appendChild(errorDiv);
      }

      /**
       * Setup provider UI based on configuration
       */
      function setupProviderUI() {
        // Ensure aiConfig exists before proceeding
        if (!aiConfig) {
          console.error('AI configuration not loaded, cannot setup provider UI');
          return;
        }
        // Clear existing options
        providerSelect.innerHTML = '';
        var hasAvailableProvider = false;

        // Add available providers
        if (aiConfig && aiConfig.fireworks_available) {
          var fireworksOption = document.createElement('option');
          fireworksOption.value = 'fireworks';
          fireworksOption.textContent = 'Fireworks.ai';
          providerSelect.appendChild(fireworksOption);
          hasAvailableProvider = true;
        }

        // If no providers are available, add disabled options
        if (!hasAvailableProvider) {
          if (!aiConfig || !aiConfig.fireworks_available) {
            var _fireworksOption = document.createElement('option');
            _fireworksOption.value = 'fireworks';
            _fireworksOption.textContent = 'Fireworks.ai (Not configured)';
            _fireworksOption.disabled = true;
            providerSelect.appendChild(_fireworksOption);
          }
          showConfigurationError('No AI providers are configured. Please check plugin settings.');
          return;
        }

        // Restore saved selections
        var savedProvider = localStorage.getItem('ai-chat-provider');
        if (savedProvider && document.querySelector("option[value=\"".concat(savedProvider, "\"]"))) {
          providerSelect.value = savedProvider;
          currentProvider = savedProvider;
        } else if (aiConfig && aiConfig.fireworks_available) {
          currentProvider = 'fireworks';
          providerSelect.value = 'fireworks';
        }
      }

      /**
       * Send a message to the AI assistant
       */
      function sendMessage() {
        var message = chatInput.value.trim();
        if (!message) {
          return;
        }

        // Check if we have a valid configuration
        if (!aiConfig || !aiConfig.fireworks_available) {
          showConfigurationError('No AI providers are configured. Please check plugin settings.');
          return;
        }

        // If no conversation is selected, create a new one
        if (!currentConversationThreadId) {
          createNewConversation().then(function () {
            // After creating conversation, send the message
            sendMessageWithConversation(message, currentConversationThreadId);
          }).catch(function (error) {
            console.error('Failed to create new conversation:', error);
            showConfigurationError('Failed to create new conversation. Please try again.');
          });
          return;
        }
        sendMessageWithConversation(message, currentConversationThreadId);
      }

      /**
       * Send message with existing conversation
       */
      function sendMessageWithConversation(message, currentConversationThreadId) {
        console.log('sendMessageWithConversation called with:', {
          message: message,
          conversationId: currentConversationThreadId
        });

        // Show results area and hide motto
        showResultsArea();

        // Add user message
        var userMessageDiv = document.createElement("div");
        userMessageDiv.className = "user-message";
        userMessageDiv.innerHTML = "<strong>You:</strong> " + message;
        messagesContainer.appendChild(userMessageDiv);

        // Save user message to database
        saveMessageToDatabase(currentConversationThreadId, 'user', message);

        // Clear input
        chatInput.value = "";

        // Show loading state
        var loadingDiv = document.createElement("div");
        loadingDiv.className = "ai-message";
        loadingDiv.innerHTML = "<strong>AI Assistant:</strong> <em>Getting your credentials for ".concat(currentProvider, "...</em>");
        messagesContainer.appendChild(loadingDiv);

        // Get user credentials via AJAX
        Ajax.call([{
          methodname: 'block_aiassistant_get_user_credentials',
          args: {
            provider: currentProvider
          },
          done: function done(credentials) {
            // Remove loading message
            messagesContainer.removeChild(loadingDiv);
            if (credentials.success) {
              sendFireworksChatMessage(message, currentConversationThreadId);
            } else {
              var errorDiv = document.createElement("div");
              errorDiv.className = "ai-message";
              errorDiv.innerHTML = "<strong>AI Assistant:</strong> <em>Error: " + credentials.message + "</em>";
              messagesContainer.appendChild(errorDiv);
            }
          },
          fail: function fail(error) {
            // Remove loading message
            if (messagesContainer.contains(loadingDiv)) {
              messagesContainer.removeChild(loadingDiv);
            }
            var errorDiv = document.createElement("div");
            errorDiv.className = "ai-message";
            errorDiv.innerHTML = "<strong>AI Assistant:</strong> <em>Failed to get credentials: " + (error.message || 'Unknown error') + "</em>";
            messagesContainer.appendChild(errorDiv);
          }
        }]);

        // Scroll to bottom
        messagesContainer.scrollTop = messagesContainer.scrollHeight;
      }

      /**
       * Send chat message to Fireworks API (Direct API approach)
       * @param {string} message - The message to send
       */
      function sendFireworksChatMessage(_x, _x2) {
        return _sendFireworksChatMessage.apply(this, arguments);
      }
      /**
       * Generate a UUID for new conversations
       */
      function _sendFireworksChatMessage() {
        _sendFireworksChatMessage = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2(message, currentConversationThreadId) {
          return _regenerator().w(function (_context2) {
            while (1) switch (_context2.n) {
              case 0:
                setTimeout(/*#__PURE__*/_asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee() {
                  var aiMessageDiv, responseSpan, url, options, response, aiResponse, retrievedDocuments, documentsProcessed, lastAIMessageContent, reader, decoder, _yield$reader$read, done, value, lines, _iterator, _step, line, data, document_sources, _iterator2, _step2, msg, currentAIContent, html_content, htmlContent, _t, _t2, _t3;
                  return _regenerator().w(function (_context) {
                    while (1) switch (_context.p = _context.n) {
                      case 0:
                        aiMessageDiv = document.createElement("div");
                        aiMessageDiv.className = "ai-message";
                        aiMessageDiv.innerHTML = "<strong>AI Assistant (Fireworks):</strong> <span class='response-text'></span>";
                        messagesContainer.appendChild(aiMessageDiv);
                        responseSpan = aiMessageDiv.querySelector('.response-text');
                        url = 'https://aimove.minesparis.psl.eu/api/chat';
                        options = {
                          method: 'POST',
                          headers: {
                            'Content-Type': 'application/json'
                          },
                          body: JSON.stringify({
                            "message": message,
                            "conversation_thread_id": currentConversationThreadId
                          })
                        };
                        _context.p = 1;
                        _context.n = 2;
                        return fetch(url, options);
                      case 2:
                        response = _context.v;
                        console.log('Fetch response status:', response.status, response.statusText);
                        if (response.ok) {
                          _context.n = 3;
                          break;
                        }
                        throw new Error("HTTP ".concat(response.status, ": ").concat(response.statusText));
                      case 3:
                        // Handle Server-Sent Events (SSE) response
                        aiResponse = '';
                        retrievedDocuments = [];
                        documentsProcessed = false;
                        lastAIMessageContent = "";
                        reader = response.body.getReader();
                        decoder = new TextDecoder();
                      case 4:
                        if (!true) {
                          _context.n = 18;
                          break;
                        }
                        _context.n = 5;
                        return reader.read();
                      case 5:
                        _yield$reader$read = _context.v;
                        done = _yield$reader$read.done;
                        value = _yield$reader$read.value;
                        if (!done) {
                          _context.n = 6;
                          break;
                        }
                        return _context.a(3, 18);
                      case 6:
                        lines = decoder.decode(value, {
                          stream: true
                        }).split('\n');
                        console.log('Received chunk:', lines);
                        _iterator = _createForOfIteratorHelper(lines);
                        _context.p = 7;
                        _iterator.s();
                      case 8:
                        if ((_step = _iterator.n()).done) {
                          _context.n = 14;
                          break;
                        }
                        line = _step.value;
                        if (line.trim()) {
                          _context.n = 9;
                          break;
                        }
                        return _context.a(3, 13);
                      case 9:
                        _context.p = 9;
                        data = JSON.parse(line);
                        console.log('Received SSE data:', data);
                        if (!(data.content === '[DONE]')) {
                          _context.n = 10;
                          break;
                        }
                        return _context.a(3, 14);
                      case 10:
                        // Handle documents (process once when available)
                        if (data.documents && Array.isArray(data.documents) && data.documents.length > 0 && !documentsProcessed) {
                          document_sources = data.documents.map(function (doc) {
                            var _doc$metadata;
                            return ((_doc$metadata = doc.metadata) === null || _doc$metadata === void 0 ? void 0 : _doc$metadata.source) || 'Unknown source';
                          });
                          showDocuments(document_sources);
                          retrievedDocuments = document_sources;
                          documentsProcessed = true;
                        }

                        // Handle content from stream_mode="values" (array of messages)
                        if (data.content && Array.isArray(data.content)) {
                          // Find the latest AI message in the messages array
                          _iterator2 = _createForOfIteratorHelper(data.content);
                          try {
                            for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
                              msg = _step2.value;
                              // Check for AIMessage type and extract content
                              if (msg.content && (msg.type === 'ai' || msg.__class__ === 'AIMessage' || typeof msg.content === 'string')) {
                                currentAIContent = msg.content; // Only update if the AI message content has changed
                                if (currentAIContent !== lastAIMessageContent) {
                                  aiResponse = currentAIContent; // Replace with latest complete AI message
                                  responseSpan.textContent = aiResponse;
                                  messagesContainer.scrollTop = messagesContainer.scrollHeight;

                                  // Convert markdown to HTML if marked is available
                                  if (typeof marked !== 'undefined' && marked.parse) {
                                    html_content = marked.parse(responseSpan.textContent);
                                    responseSpan.innerHTML = html_content;
                                  }
                                  lastAIMessageContent = currentAIContent;
                                }
                              }
                            }
                          } catch (err) {
                            _iterator2.e(err);
                          } finally {
                            _iterator2.f();
                          }
                        }
                        // Fallback for stream_mode="messages" (direct string content)
                        else if (data.content && typeof data.content === 'string') {
                          aiResponse += data.content;
                          responseSpan.textContent = aiResponse;
                          messagesContainer.scrollTop = messagesContainer.scrollHeight;
                          if (typeof marked !== 'undefined' && marked.parse) {
                            htmlContent = marked.parse(responseSpan.textContent);
                            responseSpan.innerHTML = htmlContent;
                          }
                        }
                        if (!data.error) {
                          _context.n = 11;
                          break;
                        }
                        throw new Error(data.error);
                      case 11:
                        _context.n = 13;
                        break;
                      case 12:
                        _context.p = 12;
                        _t = _context.v;
                        console.error('Parse error:', _t);
                      case 13:
                        _context.n = 8;
                        break;
                      case 14:
                        _context.n = 16;
                        break;
                      case 15:
                        _context.p = 15;
                        _t2 = _context.v;
                        _iterator.e(_t2);
                      case 16:
                        _context.p = 16;
                        _iterator.f();
                        return _context.f(16);
                      case 17:
                        _context.n = 4;
                        break;
                      case 18:
                        // Save AI response to database if we have content
                        if (aiResponse && aiResponse.trim()) {
                          console.log('Attempting to save AI response:', {
                            conversationId: currentConversationThreadId,
                            responseLength: aiResponse.length,
                            responsePreview: aiResponse.substring(0, 100) + '...'
                          });
                          saveMessageToDatabase(currentConversationThreadId, 'ai', aiResponse);
                        } else {
                          console.log('No AI response to save:', {
                            aiResponse: aiResponse,
                            conversationId: currentConversationThreadId
                          });
                        }
                        _context.n = 20;
                        break;
                      case 19:
                        _context.p = 19;
                        _t3 = _context.v;
                        console.error('FastAPI call failed:', _t3);
                        responseSpan.textContent = 'Sorry, there was an error processing your request: ' + _t3.message;
                      case 20:
                        // Final scroll to bottom
                        messagesContainer.scrollTop = messagesContainer.scrollHeight;
                      case 21:
                        return _context.a(2);
                    }
                  }, _callee, null, [[9, 12], [7, 15, 16, 17], [1, 19]]);
                })), 1000);
              case 1:
                return _context2.a(2);
            }
          }, _callee2);
        }));
        return _sendFireworksChatMessage.apply(this, arguments);
      }
      function generateUUID() {
        if (typeof crypto !== 'undefined' && crypto.randomUUID) {
          return crypto.randomUUID();
        }
        // Fallback UUID generation for older browsers
        return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {
          var r = Math.random() * 16 | 0;
          var v = c === 'x' ? r : r & 0x3 | 0x8;
          return v.toString(16);
        });
      }

      /**
       * Create a new conversation
       */
      function createNewConversation() {
        return _createNewConversation.apply(this, arguments);
      }
      /**
       * Save a message to the database
       */
      function _createNewConversation() {
        _createNewConversation = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee3() {
          var conversationId, conversationTitle, conversationList, newConversationItem, allItems, errorDiv, _t4;
          return _regenerator().w(function (_context3) {
            while (1) switch (_context3.p = _context3.n) {
              case 0:
                conversationId = generateUUID();
                conversationTitle = "New Conversation ".concat(new Date().toLocaleTimeString()); // Create new conversation DOM element using helper function
                conversationList = document.getElementById('ai-conversation-list');
                newConversationItem = createConversationElement(conversationId, conversationTitle);
                conversationList.appendChild(newConversationItem);

                // Remove active class from all items and set this one as active
                allItems = document.querySelectorAll('.ai-conversation-item');
                allItems.forEach(function (item) {
                  return item.classList.remove('active');
                });
                newConversationItem.classList.add('active');

                // Set as current conversation
                currentConversationThreadId = conversationId;

                // Clear chat messages and hide results area
                messagesContainer.innerHTML = '';
                hideResultsArea();

                // Add click listener to the new item
                setupConversationItemListener(newConversationItem);

                // Create conversation in Moodle database
                _context3.p = 1;
                _context3.n = 2;
                return createConversationInMoodle(conversationId, conversationTitle);
              case 2:
                console.log('New conversation created:', conversationId);
                _context3.n = 4;
                break;
              case 3:
                _context3.p = 3;
                _t4 = _context3.v;
                console.error('Failed to create conversation in Moodle:', _t4);
                errorDiv = document.createElement("div");
                errorDiv.className = "ai-message";
                errorDiv.innerHTML = "<strong>AI Assistant:</strong> <em>Warning: Could not save conversation. Messages may not be saved.</em>";
                messagesContainer.appendChild(errorDiv);
              case 4:
                return _context3.a(2);
            }
          }, _callee3, null, [[1, 3]]);
        }));
        return _createNewConversation.apply(this, arguments);
      }
      function saveMessageToDatabase(conversationId, messageType, content) {
        var metadata = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : '';
        console.log('saveMessageToDatabase called with:', {
          conversationId: conversationId,
          messageType: messageType,
          content: content.substring(0, 100) + (content.length > 100 ? '...' : ''),
          metadata: metadata
        });
        Ajax.call([{
          methodname: 'block_aiassistant_manage_messages',
          args: {
            action: 'save',
            conversation_id: conversationId,
            message_type: messageType,
            content: content,
            metadata: metadata
          },
          done: function done(result) {
            console.log('AJAX response received:', result);
            if (result.success) {
              console.log("Message saved successfully: ".concat(messageType, " message with ID ").concat(result.message_id));
            } else {
              console.error('Failed to save message:', result.message);
            }
          },
          fail: function fail(error) {
            console.error('Failed to save message to database:', error);
            console.error('Error details:', {
              name: error.name,
              message: error.message,
              stack: error.stack
            });
          }
        }]);
      }

      /**
       * Load messages for a conversation from database
       */
      function loadMessagesFromDatabase(conversationId) {
        return new Promise(function (resolve, reject) {
          Ajax.call([{
            methodname: 'block_aiassistant_manage_messages',
            args: {
              action: 'load',
              conversation_id: conversationId
            },
            done: function done(result) {
              if (result.success) {
                console.log("Loaded ".concat(result.messages.length, " messages for conversation ").concat(conversationId));
                resolve(result.messages);
              } else {
                console.error('Failed to load messages:', result.message);
                reject(new Error(result.message));
              }
            },
            fail: function fail(error) {
              console.error('Failed to load messages from database:', error);
              reject(error);
            }
          }]);
        });
      }

      /**
       * Create conversation in Moodle database via Ajax
       */
      function createConversationInMoodle(_x3, _x4) {
        return _createConversationInMoodle.apply(this, arguments);
      }
      /**
       * Display messages in the chat interface
       */
      function _createConversationInMoodle() {
        _createConversationInMoodle = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee4(conversationId, conversationTitle) {
          return _regenerator().w(function (_context4) {
            while (1) switch (_context4.n) {
              case 0:
                return _context4.a(2, new Promise(function (resolve, reject) {
                  Ajax.call([{
                    methodname: 'block_aiassistant_manage_conversations',
                    args: {
                      action: 'create',
                      conversation_id: conversationId,
                      title: conversationTitle
                    },
                    done: function done(result) {
                      if (result.success) {
                        resolve(result);
                      } else {
                        reject(new Error(result.message || 'Failed to create conversation'));
                      }
                    },
                    fail: function fail(error) {
                      reject(error);
                    }
                  }]);
                }));
            }
          }, _callee4);
        }));
        return _createConversationInMoodle.apply(this, arguments);
      }
      function displayMessages(messages) {
        messagesContainer.innerHTML = '';
        if (messages.length === 0) {
          hideResultsArea();
          return;
        }

        // Show results area if there are messages
        showResultsArea();
        messages.forEach(function (message) {
          var messageDiv = document.createElement('div');
          if (message.message_type === 'user') {
            messageDiv.className = 'user-message';
            messageDiv.innerHTML = '<strong>You:</strong> ' + message.content;
          } else if (message.message_type === 'ai') {
            messageDiv.className = 'ai-message';
            messageDiv.innerHTML = '<strong>AI Assistant:</strong> <span class="response-text">' + message.content + '</span>';

            // Apply markdown rendering if available
            var responseSpan = messageDiv.querySelector('.response-text');
            if (typeof marked !== 'undefined' && marked.parse) {
              responseSpan.innerHTML = marked.parse(message.content);
            }
          }
          messagesContainer.appendChild(messageDiv);
        });

        // Scroll to bottom
        messagesContainer.scrollTop = messagesContainer.scrollHeight;
      }

      /**
       * Setup event listener for a single conversation item
       */
      function setupConversationItemListener(item) {
        item.addEventListener('click', function (e) {
          // Don't trigger if clicking on delete button
          if (e.target.classList.contains('ai-conversation-delete-btn')) {
            return;
          }

          // Remove active class from all items
          var allItems = document.querySelectorAll('.ai-conversation-item');
          allItems.forEach(function (i) {
            return i.classList.remove('active');
          });

          // Add active class to clicked item
          this.classList.add('active');

          // Get conversation data
          var conversationId = this.getAttribute('data-conversation-id');
          var conversationTitle = this.querySelector('.ai-conversation-title').textContent;

          // Set as current conversation
          currentConversationThreadId = conversationId;
          console.log('Conversation selected:', {
            id: conversationId,
            title: conversationTitle
          });

          // Clear messages and show loading state
          messagesContainer.innerHTML = '<div class="ai-message"><strong>AI Assistant:</strong> <em>Loading conversation...</em></div>';

          // Load messages from database
          loadMessagesFromDatabase(conversationId).then(function (messages) {
            displayMessages(messages);
          }).catch(function (error) {
            console.error('Failed to load messages:', error);
            showResultsArea();
            messagesContainer.innerHTML = '<div class="ai-message"><strong>AI Assistant:</strong> <em>Failed to load conversation. Please try again.</em></div>';
          });
        });
      }

      // Setup conversation panel event listeners
      setupConversationPanel();

      // Add event listeners
      newConversationBtn.addEventListener('click', createNewConversation);
      sendButton.addEventListener("click", sendMessage);

      // Toggle conversations panel
      if (conversationsToggle && conversationsPanel) {
        conversationsToggle.addEventListener('click', function () {
          conversationsPanel.classList.toggle('open');
          conversationsToggle.classList.toggle('active');
        });
      }

      /**
       * Setup conversation panel functionality
       */
      function setupConversationPanel() {
        var conversationItems = document.querySelectorAll('.ai-conversation-item');
        conversationItems.forEach(function (item) {
          setupConversationItemListener(item);
        });
      }
      chatInput.addEventListener("keypress", function (e) {
        if (e.key === "Enter" && !e.shiftKey) {
          e.preventDefault();
          sendMessage();
        }
      });
    };

    // Try to initialize immediately if DOM is ready, otherwise wait for DOMContentLoaded
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initializeChat);
    } else {
      // DOM is already loaded, initialize immediately
      initializeChat();
    }
  };
});
//# sourceMappingURL=chat_interface.min.js.map
